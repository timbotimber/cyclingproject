const comments = require('./comments');
const trip = require('./trips');
const user = require('./users');
const user_trip = require('./user_trips');

module.exports = {
  comments,
  trip,
  user_trip,
  user
};